import React from 'react';
import './footer.css';

const footer = ({textoFooter}) => {
    return(
        <footer>
        <p>{textoFooter}            
        </p>
    </footer>
    )
}

export default footer